```typescript
export interface ColProps extends HTMLAttributes<HTMLElement> {
  as?: ElementType;
  className?: string;
  lg?: ColSize;
  md?: ColSize;
  sm?: ColSize;
  xl?: ColSize;
  xs?: ColSize;
  xxl?: ColSize;
}
```
